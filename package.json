{
  "name": "casimir",
  "version": "0.1.0",
  "description": "Replay, rerun and compare coding-agent sessions from Claude Code and Codex",
  "type": "module",
  "license": "MIT",
  "bin": { "casimir": "./bin/casimir.js" },
  "files": ["bin", "src", "README.md", "LICENSE"],
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test test/*.test.js",
    "lint": "node --check bin/casimir.js && for f in src/*.js src/adapters/*.js; do node --check $f || exit 1; done"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.127.0"
  }
}
